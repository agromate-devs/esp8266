void listener();

// Value that will be updated from DB

// extern float default_temperature = 0.0;
// extern float temperature_limit = 0.0;
// extern int notify_wrong_temperature = 0;    // Boolean flag

extern float default_humidity = 0.0;
extern float humidity_limit = 0.0;
extern int notify_wrong_humidity = 0;    // Boolean flag

extern float default_precipitation = 0.0;
extern float precipitation_limit = 0.0;
extern int notify_wrong_precipitation = 0;    // Boolean flag

// extern int upload_done = 0; // Boolean flag that indicates the initial query has completed